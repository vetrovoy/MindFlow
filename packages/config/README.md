# Shared Config

This package documents cross-app conventions:

- shared TypeScript baseline lives in `/tsconfig.base.json`
- frontend apps follow Feature-Sliced Design
- shared packages are consumed from app-specific FSD layers
