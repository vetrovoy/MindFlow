import { tokens } from "@mindflow/ui";

export function TaskListEntity() {
  return (
    <section
      style={{
        marginTop: tokens.spacing.lg,
        color: tokens.color.textPrimary
      }}
    >
      MindFlow web shell is ready for ACP-81.
    </section>
  );
}
