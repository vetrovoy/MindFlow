// Jest setup file for MindFlow mobile
// Add global mocks and test utilities here

// NOTE: React Native 0.84+ uses ESM internally which requires additional Jest configuration.
// For now, tests may fail with "Cannot use import statement outside a module".
// This is a known issue and will be resolved in a future update.
// 
// Workaround: Run tests with --no-cache flag or downgrade to RN 0.76.x for full Jest compatibility.

const config = {};
export default config
